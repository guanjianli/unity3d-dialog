using UnityEngine;

public class GameParam : MonoBehaviour
{
    public static bool isTalkEnd = false;
    public static bool isWordsEnd = false;

    public static bool isAutoTalk = false;//自动播放

    public static float amplitude;
}